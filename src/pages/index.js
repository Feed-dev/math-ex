import React, { useState } from "react";
import RandomEx from "components/RandomEx.js";

const IndexPage = () => {
  return (
    //apply RandomEx component in the index page
    <div>
      <RandomEx />
    </div>
  );
};

export default IndexPage;
